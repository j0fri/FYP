T = 10;
dt = [0.01, 0.1, 0.5, 1.0];
styles = ["-","--","o","-o-"];


for i = 1:length(dt)
    M = readmatrix();
            
    t = M(:,1);
    Ke = M(:,2);
    Pe = M(:,3);
    energy = readmatrix();
    semilogy(t, Pe(1)*exp(-5e-2*t));